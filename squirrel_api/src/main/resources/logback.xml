<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <property resource="application.properties"/>
    <springProperty scope="context" name="LOG_PATH" source="log.filePath"/>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <!-- encoders are assigned the type
             ch.qos.logback.classic.encoder.PatternLayoutEncoder by default -->
        <encoder>
            <pattern>%boldGreen(%date{ISO8601}) %highlight(%-5level) %yellow([%thread]) %boldMagenta(%logger{10}) %boldCyan([%file:%line]) %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH:-squirrel-api/logs}/squirrel-api.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH:-squirrel-api/logs}/%d{yyyy-MM-dd,aux}/squirrel-api.%d{yyyy-MM-dd_HH}.log.gz</fileNamePattern>
            <!-- keep 30 days'(720 hour) worth of history capped at 50GB total size -->
            <maxHistory>720</maxHistory>
            <totalSizeCap>50GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%date{ISO8601} %level [%thread] %logger{10} [%file:%line] %msg%n</pattern>
        </encoder>
    </appender>

    <logger name="com.pine.squirrel" level="info">
    </logger>

    <root level="error">
        <appender-ref ref="STDOUT" />
        <appender-ref ref="FILE"/>
    </root>
</configuration>
